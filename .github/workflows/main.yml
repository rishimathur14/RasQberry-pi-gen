name: Build Raspberry Pi Image

on:
  push:
    branches:
      - arm64
  workflow_dispatch:

jobs:
  build-image:
    runs-on: ubuntu-latest

    steps:

      - name: Install Pi-gen (Raspberry Pi OS image generator)
        run: |
          git clone https://github.com/rishimathur14/RasQberry-pi-gen.git
          cd RasQberry-pi-gen

      - name: Configure Pi-gen Build
        run: |
          cd RasQberry-pi-gen
          # Customize your Raspberry Pi OS build configuration here
          # For example, edit config or stage files to add/remove software

      - name: Build Raspberry Pi Image
        run: |
          cd RasQberry-pi-gen
          sudo ./build.sh -config  # Executes the build process
